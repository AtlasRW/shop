<% Gossip.all.each do |gossip| %>
  <div class="card">
    <div class="card-content">
      <p class="title">
        <%= gossip.title %>
      </p>
      <p class="subtitle">
        <%= gossip.user.full_name %>
      </p>
    </div>
    <footer class="card-footer">
      <a class="card-footer-item" href="<%= gossip_path(gossip.id)%>">
        <span>
          Lire le potin
        </span>
      </a>
      <a class="card-footer-item" href="<%= new_user_path %>">
        <span>
          Découvrir l'auteur
        </span>
      </a>
    </footer>
  </div>
<% end %>